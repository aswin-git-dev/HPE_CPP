apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: ecommerce
  labels:
    app: audit-service
spec:
  type: ClusterIP
  selector:
    app: audit-service
  ports:
    - name: http
      port: 8005
      targetPort: 8005

